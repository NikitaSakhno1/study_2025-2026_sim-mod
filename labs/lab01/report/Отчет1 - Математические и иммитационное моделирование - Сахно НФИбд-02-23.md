# Лабораторная работа №1
## Работа с git

**Студент:** Сахно Никита Вячеславович  
**Группа:** НФИбд-02-23  
**Дисциплина:** Математическое и имитационное моделирование  

---

## 1. Цель работы

Приобрести практические навыки работы с системой управления версиями Git.

---

## 2. Теоретическое введение

Системы контроля версий (Version Control System, VCS) применяются при работе нескольких человек над одним проектом. Обычно основное дерево проекта хранится в локальном или удалённом репозитории, к которому настроен доступ для участников проекта. При внесении изменений в содержание проекта система контроля версий позволяет их фиксировать, совмещать изменения, произведённые разными участниками проекта, производить откат к любой более ранней версии проекта, если это требуется.

В классических системах контроля версий используется централизованная модель, предполагающая наличие единого репозитория для хранения файлов. Выполнение большинства функций по управлению версиями осуществляется специальным сервером. Участник проекта (пользователь) перед началом работы посредством определённых команд получает нужную ему версию проекта, а затем отправляет на сервер внесённые изменения.

Git является распределённой системой контроля версий. В такой системе каждый пользователь имеет локальную копию репозитория со всей историей изменений. Это позволяет выполнять большинство операций локально, без постоянного подключения к серверу.

---

## 3. Ход выполнения лабораторной работы

### 3.1 Настройка SSH-доступа

Для безопасного подключения к удалённому репозиторию был сгенерирован SSH-ключ. Процесс генерации ключа представлен на рисунке 1.

Публичный ключ был добавлен в настройки аккаунта GitHub, после чего была выполнена проверка соединения с использованием команды `ssh -T git@github.com` (рис. 2).

---

### 3.2 Клонирование удалённого репозитория

После настройки SSH-доступа был выполнен процесс клонирования удалённого репозитория с помощью команды:

```bash
git clone git@github.com:yamadharma/course-directory-student-template.git
```

Результат выполнения команды представлен на рисунке 3.

---

### 3.3 Работа с локальным репозиторием

После клонирования репозитория была выполнена проверка его структуры и состояния с использованием команд `ls` и `git status` (рис. 4).

---

### 3.4 Настройка git-flow

Для организации процесса разработки и управления ветвлением был установлен и инициализирован инструмент git-flow. Проверка версии git-flow представлена на рисунке 5.

Инициализация git-flow выполнена с использованием стандартных параметров (рис. 6).

---

### 3.5 Создание релиза

Для выпуска стабильной версии проекта был создан релиз версии 1.0.0 с использованием команды:

```bash
git flow release start 1.0.0
```

Результат выполнения команды представлен на рисунке 7.

---

### 3.6 Завершение релиза

После завершения подготовки релиза была выполнена команда:

```bash
git flow release finish 1.0.0
```

В процессе завершения релиза было подтверждено сообщение merge-коммита (рис. 8).

---

### 3.7 Отправка изменений в удалённый репозиторий

После завершения релиза все изменения и теги были отправлены в удалённый репозиторий с использованием команд:

```bash
git push --all
git push --tags
```

Результат выполнения команд представлен на рисунке 9.

---

## 4. Выводы

В ходе выполнения лабораторной работы были получены практические навыки работы с системой контроля версий Git. Были изучены основные принципы работы с удалёнными репозиториями, настройка SSH-доступа, использование git-flow для управления ветвлением и выпуск релиза проекта.
